# GanttChart の特徴

ローカルファースト(バックエンドなし、単一の `index.html` 1ファイルで完結)の、初学者にもやさしいガントチャートツール。

## アシスト / ノーマルの2モード

プロジェクトごとに選べ、ヘッダーのトグルでいつでも切り替え可能。データ構造は完全共通で、切り替えても内容は失われない。

- **アシストモード(緑基調)**: 右サイドバーに「次にやること(→全部終わると『できること』)」ガイドが常設され、スケジュール追加・サブスケ追加・タスク設定・マイルストーン・メモ出しを、迷わずクリックだけで直感的に操作できる。入力欄には消えない例文プレースホルダーが付き、迷ったらそのまま真似すればよい。
- **ノーマルモード(青基調)**: 経験者向けに自由入力中心。サイドバーの代わりに**プロジェクトメモを常時表示**し、作業しながらいつでもキックオフの目的や課題・決定事項を参照できるため、作業に没頭しても目的を見失わない設計になっている。

いずれのモードでも、スケジュール/サブスケジュール/タスクの編集画面を開いたまま(モーダル表示中でも)メモの参照・編集ができる。

## 階層構造とWBS

- スケジュール → サブスケジュール → タスクの3階層(隣接リスト構造)。左のWBS一覧はレベルバッジ(スケジュール/サブ/タスク)・番号(1.1.1)・階層色で一目で区別でき、インデントは廃止して列がずれないようにしている。
- 名前の横に実稼働日数(土日・祝日を除く)を「名前(N日)」で表示。ガントバーのラベルにも同様に表示。
- 親(子を持つ行)はステータス・期間を子から自動集計(進捗%表示)。バーは移動(ドラッグで配下ごとずらす)はできるが、期間の伸縮はできない。
- 末端タスクの状態(未着手/進行中/完了)は色分けされ、WBS・ガントバー双方に反映される。

## ガントチャート操作

- バーはクリックで直接編集、ドラッグで移動・端をつまんで期間変更。ドラッグ中は半透明+スナップ済みの位置を示す「予測ゴースト」が表示され、狙った場所に置きやすい。
- タスク間の依存関係(先行→後続)を設定でき、先行の日程変更が後続へ自動でリスケジュールされる。
- マイルストーン(期間を持たない目印)、プロジェクト全体の期間(最長3年、境界線と旗で表示)、今日を示す縦線をガント上に表示。
- 表示粒度は日/週/月/四半期を横並びボタンで切り替え(月粒度でも四半期ラベルを表示)。
- 日付の目盛りに曜日を表示し、土曜=青・日曜/祝日=赤で色分け(祝日データは内蔵)。
- マウスホイールで左右スクロール、スケジュール欄とガント欄の間の専用スクロールバーで上下スクロール。

## プロジェクトメモ

- Markdown対応の複数メモを管理でき、メモごとに名前を付けて一覧から切り替えられる。内容のあるメモはプレビューから、新規メモは編集から開く。
- 箇条書き・チェックリスト行でEnterすると次の項目を自動継続(Teamsのような書き心地)。
- `.md`ファイルの読み込み/書き出しにも対応。

## 履歴・安全網

- 操作は基本的に元に戻せる(Ctrl+Zまたはヘッダーの「元に戻す」)。
- 変更履歴は「追加(テーマ色)/変更(オレンジ)/削除(赤)」に色分けされ、それぞれ「何を何に変えたか」を記録。任意の時点まで巻き戻すこともできる(この巻き戻し自体も取り消し可能)。

## タスク管理・出力

- サブスケジュールから「タスク管理」パネルを開くと、配下タスクの状態変更・追加・編集・削除がリスト形式でまとめて行える。
- 出力(入出力メニュー)は3種類: 印刷によるPDF/画像出力(共有用)、JSONでのバックアップ/復元、Excelで開けるCSV出力。
- 新規プロジェクトはテンプレート(チーム開発の例)か空の状態から作成でき、`.md`メモの読み込みや期間設定も同時に行える。
- ダークモード対応(ヘッダーの 🌙/☀ で切り替え、選択は保存される)。差分は背景系の色のみで、テーマカラーや情報の色分けはライトと共通。

---

# 画面とボタンの説明

画面は「ヘッダー」「進捗バー」「スケジュール欄(左)」「ガント欄(中央)」「ガイド/メモ(右)」で構成される。

## ヘッダー(左から順に)

| ボタン / UI | 効果・開くもの |
|---|---|
| プロジェクト名のドロップダウン | プロジェクトの切り替え。選ぶとそのプロジェクトの全データを読み込み直す |
| ＋ 新規 | **新規プロジェクト作成モーダル**を開く(名前・モード・期間(最長3年)・テンプレート・メモの初期入力/.md読み込み) |
| 📝 メモ | **プロジェクトメモパネル**(右端ドッキング・非モーダル)の開閉。※ノーマルモードでは常時表示のためこのボタンは出ない |
| ⚙ | **プロジェクト設定モーダル**を開く(名称変更・期間変更・プロジェクト削除) |
| アシスト / ノーマル | モードの切り替え。テーマ色(緑⇔青)と右ペインの内容(ガイド⇔常時メモ)が切り替わる。データは共通 |
| ＋ マイルストーン | **マイルストーン追加モーダル**を開く(名称・日付)。追加するとその日付へ自動スクロール |
| 日 / 週 / 月 / 四半期 | ガントの表示粒度を即時切り替え(ズームに相当)。月粒度では四半期(Q)ラベルも表示 |
| ↶ 元に戻す | 直前の変更を取り消す(Ctrl+Zと同じ)。取り消せる変更がないときはグレーアウト |
| 履歴 | **変更履歴パネル**(右からスライド)を開く。追加=テーマ色/変更=オレンジ/削除=赤で色分けされ、各項目の「この時点に戻す」でその状態へロールバックできる |
| 入出力 | **入出力/バックアップモーダル**を開く(🖨 印刷/PDF保存・📊 Excel形式CSV・⬇ JSON書き出し・⬆ JSON読み込み=新規プロジェクトとして復元) |
| 🌙 / ☀ | ライト/ダークモードの切り替え。選択は保存され次回も引き継がれる |

## 進捗バー(ヘッダー下)

全体の進捗%(完了=1・進行中=0.5で加重)、タスクの完了数、状態色の凡例を表示する。表示専用で操作はない。

## スケジュール欄(左ペイン)

- **＋ スケジュール** — スケジュール(フェーズ)の追加モーダルを開く。
- 各行の構成: 折りたたみ▼/▶(子を持つ行のみ)、状態ドット、番号(1.1.1)、レベルバッジ(スケジュール/サブ/タスク)、名前(稼働日数)、期間、状態(親=進捗% / タスク=未着手・進行中・完了)。
- **行の名前クリック / ✎(ホバー)** — その項目の**編集モーダル**を開く。
- **＋(ホバー)** — その行の下位(スケジュール→サブ、サブ→タスク)を追加するモーダルを開く。
- **🗑(ホバー)** — 配下ごと削除(確認ダイアログあり)。
- **☑ n(サブスケジュール行)** — **タスク管理パネル**を開く。配下タスクの状態変更(○3つ)・編集・削除・追加がまとめて行える。
- 行クリックで選択(ガント側の行もハイライト)。

## 中央の細いスクロールバー

スケジュール欄とガント欄を**上下同期**でスクロールする(つまみをドラッグ、またはレール部クリック)。

## ガント欄(中央)

- **日付ヘッダー** — 日付と曜日(土=青/日祝=赤)。下端の旗: 「今日」(赤)、「◆ マイルストーン」(紫・**クリックで編集モーダル**)、「計画開始/計画終了」(テーマ色・プロジェクト期間)。
- **バーのクリック** — その項目の編集モーダルを直接開く。
- **バーのドラッグ** — 期間の移動。ドラッグ中は半透明+確定位置を示すゴースト+日付ツールチップが出る。**両端をつまむと期間の伸縮**(タスクのみ)。親バーは移動のみ(配下ごと一括でずれる)。
- **依存線(矢印)** — 先行→後続の関係を表示(設定はタスクの編集モーダルの「先行タスク」から)。
- **マウスホイール** — 左右スクロール。
- バーの見た目: 親=濃いテーマ色/サブ=グレー/タスク=状態色(未着手=淡グレー・進行中=アンバー・完了=淡いテーマ色+✓)。

## 右ペイン(アシストモードのみ)

- **次にやること** — 手順のチェックリスト。**各カードをクリックすると対応する追加モーダルが直接開く**。全部✓になると「**できること**」(スケジュール追加/サブスケ追加/タスク設定/マイルストーン選択/メモ出し)に切り替わる。

## プロジェクトメモパネル(📝 で開閉、ノーマルは常時表示)

- 左が**メモ一覧(蓄積欄)**: 「＋ 新規メモ」で追加、クリックで切り替え。
- 右が編集エリア: メモ名の変更、**編集/プレビュー**タブ(内容のある既存メモはプレビューから開く)、📄読込(.md)・📄書出(.md)・削除・保存。
- 箇条書き・チェックリスト行でEnterすると次の項目を自動継続。
- 非モーダルなので、**開いたままガント操作や編集モーダルの入力ができる**。

## 編集モーダル(スケジュール/サブスケジュール/タスク共通)

- 名称、**所属先の付け替え**(サブ・タスクのみ)、開始日/期間(稼働日)/終了日(どれかを入れると残りを自動計算)、担当者、状態(タスクのみ・親は自動集計)、**先行タスク**(タスクのみ・チェックで依存関係を設定)、メモ・目的。
- フッター: **削除(左端・確認あり)** / キャンセル / **保存(右端)**。保存ボタンは下端に固定されスクロール不要。
- アシストモードでは各入力欄に消えない例文ヒントが表示される。
