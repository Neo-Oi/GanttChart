# 仕様書 (ドラフト)

このドキュメントは GanttChart の機能仕様を段階的にまとめていくための作業ファイルです。会話を重ねながら追記・修正していきます。未確定の項目には「(要確認)」を付けています。

## 1. 概要

新しくガントチャート/プロジェクト計画ツールを作る。ターゲットは「ガントチャートの使い方も書き方も分からない初学者」で、迷わず使えることを最優先にする。

## 2. ペルソナ

- これからチームに入って働き始める初学者
- ガントチャート自体に馴染みがなく、「何を」「どの順番で」書けばいいのか分からない
- スケジュール管理の経験が浅いため、専門用語や自由入力だけのUIだと詰まりやすい

## 3. モード

2つの利用モードを用意する。

### 3.1 ノーマルモード
- 経験者向け。自由入力中心の、通常のガントチャートUI。

### 3.2 アシストガントモード
- 初学者向け。入力の型・書き方の例を常に見せながらスケジュールを作らせる。
- 詳細は4章。

【確定】モードはプロジェクトごとに選び、ヘッダーのトグルでいつでも切り替え可能とする。プロジェクト作成時に固定したりアカウント全体で1つに決めたりはしない。モード切替はUI/ガイド表示の切り替えであり、データ構造(スケジュール/サブスケジュール/タスク)自体は両モードで共通(4.3・5参照)なので、切り替えてもデータが変換されたり消えたりすることはない。

## 4. アシストガントモードの詳細

### 4.0 原体験とサンプルシナリオ(チーム開発)

ユーザー自身の実体験より: チーム開発の大枠は「定義 → 設計 → 実装 → フォロー」だが、
- 「定義」の中には 機能要件定義 / 非機能要件定義 がある
- 「設計」の中には 基本設計書 / 詳細設計書 がある
- 「実装」も機能単位などで区切って進める

—という具合に、**大分類(フェーズ) → 中分類(成果物・工程) → 具体的な作業**という3階層になっていて、「具体的に何を書いて、どう管理していけばいいか分からなかった」という原体験がある。この3階層を、そのままスケジュール/サブスケジュール/タスクの階層に対応させる。

| 階層 | 役割 | 例 |
|---|---|---|
| スケジュール(第1階層) | フェーズ | 定義 / 設計 / 実装 / フォロー |
| サブスケジュール(第2階層) | 成果物・工程 | 定義→「機能要件定義」「非機能要件定義」/ 設計→「基本設計書作成」「詳細設計書作成」/ 実装→「機能A実装」「機能B実装」のように区切る |
| タスク(第3階層) | 具体的な作業 | 「機能要件定義」→「ヒアリングメモ作成」「要求一覧作成」「レビュー依頼」など |

このシナリオを、以下4.1・4.2の例文やガイド文言の土台として使う(初学者にとって最も刺さる実例のため)。

### 4.1 例文プレースホルダー(消えないプレースホルダー)

- 各入力欄に「書き方の例」を薄いグレーの文字で表示する。
- 通常のHTML `placeholder`(入力すると消える)とは別に、**例文は入力後も残り続ける**補助テキストとして表示する。
  - 実装イメージ: 入力欄の下または横に「例: ○○○○」という薄字のキャプションを常設し、これは入力欄の値がある/なしに関わらず常に表示する。
  - こうすることで、ユーザーは自分の入力と見本を並べて見比べながら書き進められる。
- 対象フィールドの例(4.0のシナリオをベースにした具体例。要確認: 実際にどの項目に例文を出すか確定する):
  - スケジュール名(第1階層): 例:「定義」「設計」「実装」「フォロー」
  - サブスケジュール名(第2階層): 例:「機能要件定義」「基本設計書作成」
  - タスク名(第3階層): 例:「ヒアリングメモ作成」「要求一覧作成」
  - 開始日・終了日: 例:「2026-08-01 〜 2026-08-15」
  - 担当者: 例:「田中さん」
  - メモ・目的: 例:「機能要件と非機能要件を分けて書く。非機能要件は性能・セキュリティなど」

(要確認: この「消えない例文」の見た目・配置を実際にどうするか。上記はあくまで一案です。)

### 4.2 次に何をすべきかの道筋提示

- スケジュール(親レベル)を書き終えたあと、迷わず次のステップに進めるよう、常時表示のガイド/チェックリストを出す。
- 想定する流れと文言案(4.0のシナリオに準拠):
  1. **スケジュールを書く**(フェーズを並べる)— 完了時の一言: 「まずは大きな流れ(例: 定義・設計・実装・フォロー)を書けました」
  2. **サブスケジュールを追加する**(成果物・工程に分ける)— 促し文言: 「次は各フェーズの中身を分けましょう。例:「定義」なら"機能要件定義"と"非機能要件定義"のように、成果物単位で分けます」
  3. **タスクを設定する**(具体的な作業に分解する)— 促し文言: 「最後に、実際に手を動かす単位まで分解しましょう。例:"ヒアリングメモ作成"のように」
  4. (以降、必要に応じてマイルストーン・コメントなど)
- 各ステップは完了したら✓が付き、次にやることが強調表示される、といった進捗ガイド形式を想定。

【確定】表示形式は**サイドパネル常設のチェックリスト**とする。完了ごとに割り込むポップアップ/モーダルは出さない — 常に画面の隅に置いておき、初学者が自分のペースで確認できるようにする(操作を強制的に中断させない)。大きな節目(例: スケジュールを書き終えた瞬間など)でチェックリスト側の該当項目がハイライトされる程度の演出は可。

### 4.3 【確定】タスクはスケジュール階層の第3階層(案A採用)

4.0のシナリオの通り、**タスクはスケジュール/サブスケジュールと地続きの第3階層**として扱う(定義→機能要件定義→ヒアリングメモ作成、という一本のドリルダウン)。

- ガントの木構造は「スケジュール(第1階層) → サブスケジュール(第2階層) → タスク(第3階層、末端)」の3階層固定。
- ガントとは無関係な独立ドメインとしての「タスク管理(かんばん)」は持たない。タスクはあくまでこのガント階層の末端ノードであり、スキーマ上もスケジュール/サブスケジュールと同じ木構造(隣接リスト、`parentId`)で表現する。
- 【確定】末端のタスクには**ステータス(未着手/進行中/完了)**を持たせる。ガントバーはモードのアクセント色の**濃淡**で表現し(未着手=濃 → 進行中 → 完了=淡)、テーマと調和させつつ状態を見分けられるようにする。
- 【確定】スケジュール/サブスケジュール(親レベル)には**手動でステータスを持たせず、子タスクの状況から自動集計**する(全 未着手→未着手 / 全 完了→完了 / それ以外→進行中)。サマリーバーは中立の灰色で、スケジュール=淡グレー・サブスケジュール=濃グレーと濃さを変えて階層を区別する。
- 【確定・実装済み】タスクはガント階層(スケジュール→サブスケジュール→タスク)に入れ子で表示されると同時に、各サブスケジュールから開ける**タスク管理パネル**(サイドパネル)でも一覧・追加・状態変更・削除ができる(表示と集中編集の両立)。かんばんのような独立ドメインは持たず、あくまで同じ木構造の末端ノード。

## 5. データ構造はモード間で共通(確定)

【確定】アシストガントモードとノーマルモードは、**同じデータ構造(スケジュール/サブスケジュール/タスクの3階層ツリー)の上に成り立つ**。両モードの違いはUI/ガイド表示だけ(例文プレースホルダーや次にやることガイドの有無)であり、データそのものに違いはない。3章の通りいつでも切替可能なため、これはモード切替時にデータが失われたり変換されたりしないための前提でもある。

## 6. 見た目の方針(トンマナ)

【確定】安っぽく見えないこと。個人開発の局所最適なUIになりがちなポイントとして、配色・余白・フォントなどの質感には常に注意を払う。具体的なビジュアルデザイン(配色の詳細、フォント、コンポーネントのスタイル等)は後日UIモックアップの段階で詰めるが、「チープにしない」という前提そのものは今のうちに確定させておく。

【確定】モードごとに基調色を分ける:
- アシストガントモード(初学者向け) → **緑系**を基調色にする
- ノーマルモード(経験者向け) → **青系**を基調色にする

ヘッダーやアクセントカラーにこの基調色を使うことで、今どちらのモードで作業しているかが一目で分かるようにする。

(要確認・詳細未定 — ユーザーの希望により後回し、UIモックアップ時に詰める)
- 具体的な配色(トーン・彩度・ダークモード対応など)、フォント、全体的なデザインシステム
- 4.1の例文プレースホルダーの具体的な見た目・配置
- タスクのステータス色分け(4.3: 未着手=グレー、進行中=水色、完了=緑)と、ノーマルモードの基調色(青系)・アシストモードの基調色(緑系)との衝突がないような配色調整(特に「完了」の緑とアシストモードの基調色の緑が重ならないよう、明度・彩度で書き分けるなどの工夫が要る)

## 7. タスク/スケジュール間の依存関係

【確定】依存関係(「前工程が終わらないと次が始まらない」という関係)を持たせる。ガントチャートの中心機能の一つとして、前後関係を線でつなげられるようにする。

【確定】依存関係は**タスク同士のみ**で結べる(スケジュール/サブスケジュール同士は結ばない)。シンプルさを優先し、粒度の細かいタスクレベルだけに限定する。

【確定】依存元タスクの日程が変わったときは、**依存先タスクの日程も自動で連動してずれる**(自動リスケジュール)。

(要確認・詳細未定)
- 自動リスケジュールの具体的な挙動(依存元が伸びたら依存先の開始日をそのままスライドするのか、間に猶予日数を挟めるようにするのか等)
- 初学者向けアシストモードでは、依存関係の設定自体もガイド・例文で補助するか(4章の仕組みと合わせて検討)
- サブスケジュール/スケジュールの期間(バー)は、配下タスクの依存関係によるリスケジュールの結果を反映して自動で伸縮するか(親レベルのステータス自動集計と同様の考え方になりそう)

## 8. 期間の入力方法

【確定】開始日・終了日のどちらからでも、また期間(日数)からでも入力できるようにし、入力していない側は自動計算する(例: 開始日+日数を入れると終了日が自動で決まる。逆に開始日・終了日を入れると日数が自動表示される)。初学者は「何日間かかるか」で考えることが多い一方、経験者は具体的な日付で管理したい場合もあるため、両方をサポートする。

【確定】日数計算は**稼働日ベース**(土日・祝日を除く)とする。実務の感覚(「実働何日」)に近く、依存関係の自動リスケジュール(7章)とも整合させやすい。

【確定】祝日データは**日本の祝日を内蔵し、ユーザーの入力なしで自動適用**する(稼働日=平日かつ祝日でない日)。

(要確認・詳細未定)
- 内蔵する祝日データの出典・更新方法(法改正で祝日が変わることがあるため、年ごとのデータ更新の仕組みが要る)
- ユーザー自身が個別の休日(夏季休暇など)を追加できるようにするか(将来拡張)
- 土日以外の曜日を稼働日から外したいケース(シフト制など)に対応するか、まずは「土日+祝日」固定でよいか

## 9. マイルストーン

【確定】マイルストーン機能を持たせる。期間を持たない、特定の日付に立てる目印(納期、レビュー会など)としてガント上に表示する。

(要確認・詳細未定)
- マイルストーンはどの階層(スケジュール/サブスケジュール/タスク)に対して設定できるか
- アシストモードの「次にやることガイド」(4.2)にマイルストーン設定のステップを含めるか

## 10. 複数プロジェクト管理

【確定】1つのブラウザ内で複数のプロジェクト(ガントチャート)を作成し、切り替えられるようにする。仕事で複数の案件を抱えるのは自然なことなので、最初から複数プロジェクト対応で設計する。

(要確認・詳細未定)
- プロジェクト一覧の見せ方(ホーム画面的なプロジェクト選択画面を作るか)
- モード(3章)はプロジェクトごとの設定なので、プロジェクトを切り替えるとモードの状態も切り替わる、という理解でよいか

## 11. アシストモードのプロジェクト作成: テンプレート

【確定】アシストモードで新規プロジェクトを作るとき、真っ白な状態からではなく、**テンプレートを選んで始められる**ようにする。4.0の「チーム開発(定義/設計/実装/フォロー)」シナリオを最初のテンプレートとして用意し、選ぶとスケジュール/サブスケジュールがあらかじめ並んだ状態から編集を始められる。

(要確認・詳細未定)
- 最初に用意するテンプレートは「チーム開発」1種類だけでよいか、複数用意するか
- テンプレートを選ばず真っ白から始める選択肢も残すか(4.1の例文プレースホルダーはその場合の補助として機能する)

## 12. 共有・エクスポート

【確定】ローカル完結(IndexedDBのみ、バックエンドなし)の方針は維持しつつ、作成したスケジュールを**画像またはPDFとして書き出す**機能を持たせる。チームに入ったばかりの初学者が、上司やチームに完成したスケジュールを見せる場面を想定している。共同編集やクラウド共有は現時点ではスコープ外(将来的な検討事項として残す)。

【確定】ブラウザのキャッシュ削除・機種変更でのデータ消失に備え、**プロジェクトデータ全体をJSONとしてエクスポート/インポートする**バックアップ機能を持たせる。画像/PDF出力(閲覧・共有用)とJSONエクスポート(バックアップ・復元用)は別の目的の機能として両方用意する。

## 14. 今日マーカー

【確定】ガント上に「今日」を示す縦線(トデイマーカー)を表示する。計画に対して今どこにいるかが一目で分かるようにする。

## 15. 表示粒度の切り替え

【確定】ガントの表示粒度(日/週/月/四半期)を切り替えられるようにする。短期プロジェクトは日単位、長期プロジェクトは月・四半期単位で俯瞰できるようにする。

(要確認・詳細未定)
- 粒度の切り替えはプロジェクトごとの設定か、都度その場で切り替えるビュー操作か
- 粒度が変わっても稼働日ベースの日数計算(8章)自体は変わらない、という理解でよいか(表示上のグリッド線が変わるだけ)

## 16. ガントバーの直接操作

【確定】ガントバーは**ドラッグ操作(移動・端をつまんで期間変更)とフォーム入力の両方**に対応する。経験者はドラッグで素早く、初学者はフォーム+例文プレースホルダー(4.1)で確実に編集できるようにする。

(要確認・詳細未定)
- ドラッグ操作は依存関係(7章)の自動リスケジュールとどう連動するか(ドラッグ中にリアルタイムで依存先もずれて見えるか、ドロップ確定後にまとめて反映するか)
- ノーマルモードとアシストモードでドラッグ操作の可否自体を変えるか、両モードで共通にするか

## 17. 元に戻す(undo)・変更履歴

【確定】操作ミスに備え、**元に戻す(undo)・変更履歴**を持たせる。初学者は操作を誤りやすいため、安全網として優先度高めの機能とする。

(要確認・詳細未定)
- undoの範囲(直前の1操作だけか、複数手前まで遡れるか)
- 変更履歴をどのドメイン(スケジュール/サブスケジュール/タスク、マイルストーン、依存関係など)に対して記録するか

## 18. 実装フェーズとの関係

実装フェーズはこの仕様書の内容が固まってから改めて決める。決まり次第 CLAUDE.md に反映する。
